import { Component } from "react";
import './navbar.css';

class Navbar extends Component {

    render() {
        return(
            <nav class="main-navbar">
                <div class="wrap">
                    <span>LOGO</span>
                </div>
            </nav>
        );
    }

}

export default Navbar;
